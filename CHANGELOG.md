# 变更记录

## Unreleased — 2026-09-03

### 课堂交付

- 发布当前16页课程大纲、逐页文字与讲师讲稿，并校正为连续90分钟。
- 清除P2重复实践提示；P12—P14按定稿统一为自动化实验、科研工具接入、假设与路线迭代三段，历史页不再新增课堂操作。
- 发布六个原子 Skill 的安装脚本、环境自检命令和Windows/Ubuntu持续集成。
- 增加可追溯的结课证据包，不把课程运行写成外部科学验证。
- 基线入口为子进程固定使用UTF-8，避免英文版Windows终端输出中文进度时中断。
- 课程基线Skill改用`nanoscigpt-research-baseline-builder`命名空间，避免与本机同名通用Skill混用；同时明确CSV/LAMOST可运行，FITS与其他格式暂只做接入设计。

### 运行与研究状态

- AI Scientist v1 默认拒绝覆盖已完成材料；只有显式 `--overwrite` 才替换本流程生成的文件。
- AI Scientist v1 的研究问题、短稿和图表使用“预训练步数”“验证损失”等课堂用语，同时保留原始字段用于追溯。
- AI Scientist v2 在进程失败、记录缺失或指标缺失时持久化失败原因。
- AI Scientist v2 的树状态、状态命令和取舍文件增加课堂用语，并将最终规则写成可直接讲解的中文。
- 序列示例的两条研究路线分别改变训练轮数和上下文长度；尚无第二个安全控制变量的结构化示例停在 `design_only`。

### 自定义科学数据

- 十类内置领域改由同一注册表声明。
- 基线CLI新增单条数值时序CSV入口，可指定时间列排序并真实运行GRU教学基线。
- 蛋白质数据准备新增`--fasta`入口，并在运行报告中保留用户文件身份；课程组成分类继续明确为教学标签，不冒充蛋白质功能标签。
- 序列课堂入口新增`--skip-downstream`，允许只有无标签序列时只完成预训练和采样。
- DNA自备FASTA现在把绝对来源路径与`user_file`身份写入运行报告；子进程失败时先显示真实标准输出和错误，再停止课堂流程。
- SMILES数据准备新增`--csv`和`--smiles-column`；无性质标签时可只做预训练，不借用内置ESOL标签。
- 增加自定义领域接入清单，要求明确数据读取、科学表示、预训练目标、下游任务和评价器。
- 增加领域课程卡和 `--describe` 入口，避免助教把遮盖重建误说成下一步预测。
- 通过GLM-5.3真实CLI对话收紧Scientific Language Skill：自有数据与内置样例分开，一次只推进一个问题或动作。
- text课堂样例现在真实更新并另存微调参数；科学样例仍明确使用冻结表示的下游任务。
