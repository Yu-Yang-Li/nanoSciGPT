# 自定义科学领域接入清单

复制本目录作为自己的接入记录，并逐项补全。五项未完成前，不把新领域写成“已经支持”。

## 1. 数据读取

- 原始文件：
- 一个样本的含义：
- 缺失和异常处理：
- 训练/验证数据如何得到：

## 2. 科学表示

- 模型处理的基本单位：
- 必须保留的顺序、邻域、几何、周期或尺度关系：
- 可目视检查的表示样例：

## 3. 预训练目标

- 不使用下游标签时预测什么：
- 损失怎样计算：
- 极小样本运行结果：

## 4. 下游任务

- 分类、回归、生成或模拟：
- 标签从哪里来：
- 预训练表示怎样接入：

## 5. 评价器

- 主指标与方向：
- 与哪个基线比较：
- 继续或停止的门槛：
- 失败结果保存在哪里：

完成后，再在 `nanoscigpt/domains/registry.py` 添加一个 `DomainSpec`，并为数据检查、最小训练和评价器各补一项测试。
