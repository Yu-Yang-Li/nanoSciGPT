# AutoResearch Model Iteration Skill｜GLM-5.3 CLI实测

## 测试设置

- Codex CLI：0.152.0
- 模型：`scnet/GLM-5.3`
- 贯穿对象：仓库内置蛋白质课程样例
- V0：`smoke`配置，真实完成预训练、采样和冻结表示下游任务

## 学生提出“继续深入迭代优化”

GLM-5.3读取Skill和真实`run_report.json`后回答：

> 我先复述你的 V0：蛋白质 smoke 训练已完成，UniProtKB 教学子集 450 条训练、50 条验证，CPU 上跑 1 层、1 头、16 维小模型，仅训练 2 步，用时约 11.5 秒，下游任务也已完成。
>
> 下一步建议不急着“深入”改很多处，而是做一次受控迭代：同一数据、同一模型结构和评价项，只把训练步数增加一倍。我先只生成这份迭代设置给你确认，不启动新训练；你同意后我们再运行，并同口径比较 V0 和 V1。

这次回答没有重复询问领域、数据或目标，也没有把“深入优化”理解成同时更换模型、数据和指标。

## 真实运行结果

先运行`--plan_only`，`iteration_spec.json`记录：

- 保持数据、模型结构、批量大小、上下文长度和评价方式不变；
- 只把`max_iters`从2改为4；
- 计划阶段没有生成候选模型。

学生同意后再运行候选，结果为：

- V0 `best_val_loss`：3.051049；
- V1 `best_val_loss`：3.051049；
- 改善：0.0000，未达到0.05门槛；
- 下一步：`stop_branch`。

`candidate_run_report.json`、`comparison.json`和`research_state.json`均已真实生成。这个负结果正好适合课堂说明：多训练两步不必然带来改善，闭环的作用也包括留下停止依据。

## 当前判断

该Skill已经能稳定承接一份完成的nanoSciGPT V0，先计划、后授权、再运行，并用同一指标决定保留或停止。它只处理一条计算路线的一次受控改动，不包含文献检索、写稿或多路线探索。
