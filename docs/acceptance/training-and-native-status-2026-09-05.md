# 三个教学Skill：已运行的部分与尚未完成的部分

更新日期：2026-09-05。本轮只打磨Skill及其数据、代码和验收，不改PPT。

发布复查：[186项全量通过，361.08秒；干净克隆的README基础流程全部完成](publish-2026-09-05/README.md)。独立虚拟环境复用本机依赖，并非全新电脑安装验收；原版研究失败状态不因此升级。下方接口核查和较早测试保留为历史证据。

最新[流式改码对照](native-stream-2026-09-05/README.md)：保留GLM、原版实验提示词、研究想法和固定数据，6.609秒收到首条响应，持续至219.719秒；这次未出现200秒网关等待失败，但以`length`结束，正文2字符、补丁为空。新实验仍未执行，不能称作v1已跑通。父进程300秒总时限未触发，无自动重试或全局代理修改。本轮相关23项测试通过（14.32秒）；下列186项是上一轮全量结果，本次不冒称全量重跑。

此前[分层核查](provider-boundary-2026-09-05/README.md)已定位两件不同的问题：CLI异步提问的参数错误与等待方式；模型关闭推理参数在代理中丢失且直连SCNet也不被接受。改为命令行普通问答后，相同GLM学生输入两轮均正常结束；回答质量仍待打磨，不作全面通过。保留原研究输入的v1非流式长请求在约204秒返回网关超时，未产生代码；随后流式结果以上方新记录为准。

当前全量回归：**186项通过，380.64秒，0失败、0跳过**，见[最新完整报告](provider-boundary-2026-09-05/full-tests.xml)。三个Skill格式检查通过。下文185项及更早数字只作历史记录，不与最新结果累加。

用户确认继续后，已完成[受限沙箱修复和真实读文件检查](cli-sandbox-repair-2026-09-05/README.md)，并增加[原版v1失败保留和失败状态修补](native-failure-repair-2026-09-05/README.md)。默认模型的新会话已实际运行LAMOST基线及蛋白质预训练/微调；这两组保留本机记忆访问，不冒充严格的无历史上下文验收。记录脚本现已每轮关闭持久记忆，新的默认模型与GLM多轮验收仍须继续。

随后[默认模型的私有蛋白质两轮对话](cli-after-repair-2026-09-05/private-ai-scientist-default/dialogue.md)做到先了解评分步骤、再询问训练目标，并保持仅讨论不执行。GLM仍出现过多追问、不当统计推断和提问工具参数错误，不能称为稳定兼容。原版v1新增[实际关闭推理参数的探针](native-coder-thinking-2026-09-05/README.md)仍被截断，未产生代码。没有用默认模型的对话通过替代GLM或完整原版研究验收。

此前完成[v1 显式兼容接口路由修复](native-api-routing-2026-09-05/README.md)：GLM 单次与连续多回答、默认模型调用均实际通过；编程与两个评阅入口统一采用显式选择的模型。相关 17 项回归通过。修复不涉及长请求与整场预算，尚未验收完整自主实验或课堂对话。

失败处理和对话捕获修改后的全量检查：**185项通过，389.30秒，0失败、0跳过**，见[新全量报告](native-failure-repair-2026-09-05/full-tests.xml)。新一轮[真实CLI记录](cli-after-repair-2026-09-05/README.md)单独报告各场景，不与代码测试混算；这批对话暴露的Skill措辞调整发生在全量测试之后，另作格式、入口回归和真实回复检查。

最终又补上对话捕获超时处理并调整Skill引导，相关[32项回归通过（13.88秒）](native-failure-repair-2026-09-05/final-regression.xml)，Skill格式检查通过。185项全量运行发生在这项最终补丁之前，不将旧全量测试写成最终状态全部重跑。

失败处理修补之前的全量检查：**176 项通过，356.97 秒，0 跳过**，见[当时的完整测试报告](native-api-routing-2026-09-05/full-tests.xml)。这是当时仓库全部 `tests/` 的代码验收，不能代替新增修改的回归、原版 Agent 研究与学生连续 CLI 对话。

## 较早的验收记录

此前[原项目模型调用与想法反思实测](native-api-2026-09-05/README.md)：v1 使用默认模型实际生成并反思修改了 1 条方案；v2 的默认模型与 GLM 工具调用通过，默认模型图像读取通过。当时未适配的 v1 拒绝 GLM 模型名，该路由问题已在上面的新一轮检查中修复。这些阶段结果不代表实验改码、完整写作、v2 研究树和学生 CLI 连续对话已通过。

随后用原版 v1 实验提示词和 Aider 实际尝试改码，遇到请求超时与输出截断，**没有产生可执行的新实验代码**；已保存失败原文及固定输入摘要。短路径独立环境解决了安装路径错误，但不是冷环境兼容性已通过。诊断进程现已全部结束，不继续无界重试，也不将这些失败写成课堂通过。

此前[课堂交付复核](release-gate-2026-09-05/README.md)：当时全量165项通过，随后入口修正的7项回归通过；已校验三个Skill、数据清单和隔离安装。README、讲师导航与安装命令不再默认导向旧规则演示。原版Agent完整研究、CLI连续教学和当前版本GitHub克隆验收仍未完成。

后续已增加[学生自有蛋白质CSV接入实测](student-protein-2026-09-05/README.md)：数值标签回归、无标签只预训练，保留来源及实际失败结果。其他自有数据接口和完整CLI/原项目研究验收仍未完成。

此前补齐[微调任务与原版v1实验接口的往返实测](native-supervised-roundtrip-2026-09-05/README.md)：保留原任务、标签、任务头和模型；已运行蛋白质CSV回归与ESOL回归。监督实验模板是教学适配，使用原版GPT类和研究调用接口，并非原版Agent研究全流程。十类样例再次任务微调也增加了状态延续检查。

监督任务接续阶段的较早全量回归：**152 项通过，377.22 秒**，见[XML记录](native-supervised-roundtrip-2026-09-05/regression.xml)。当时三个 Skill 格式检查通过。下文143项/补测记录也保留作历史，不与之后的测试数相加。

随后修正[课后证据包导出](evidence-pack-2026-09-05/README.md)：无标签跳过不再误写已评测，支持最新微调结果和不改格式的原项目附件，保留退步结果与未记录的决定。导出相关16项测试通过；两份现有真实运行已实际导出。152项全量检查发生在这次修正之前，本次只做相关回归，不把旧全量结果写成新版本全量验收。

此前[定位CLI沙箱根因](cli-sandbox-diagnosis-2026-09-05/README.md)：全局`deny_read_acl_state.json`为22个空字节，解析失败；仓库与干净临时目录均复现。旧续聊曾变成全权限，后修正记录脚本每轮保持受限沙箱、默认模型不再硬编码。历史失败记录保留；此故障现已按上面的修复记录解除，不再列为等待授权。

## 当前结果

| 学生要做的事 | 实际运行情况 | 仍需说明 |
|---|---|---|
| 用LAMOST课程数据做参数回归 | 已运行RandomForest，产生实际评价文件 | 只预测课程提供的有效温度，不代表全部恒星参数 |
| 选择一种科学数据做小规模预训练 | 十类样例逐个实际运行完成 | 六类结构化数据是生成的教学样例，不是观测或实验数据 |
| 接着预训练模型做下游微调 | 十类样例均实际更新了预训练参数，另存模型，原文件保持不变 | 蛋白质和SMILES这次指标变差，未筛掉；不是预训练收益的严格对照实验 |
| 文本由接龙转向回答 | 已运行只对回答部分计算误差的微调 | 200步后能记住八道原题，换个问法仍回答不好，不能称作通用问答模型 |
| 将课程模型交给原版v1继续实验 | 文本、蛋白质、DNA、SMILES的实际预训练权重均已接续运行原版实验代码 | 限这四类因果序列GPT；重新建立优化器，不恢复原优化器状态 |
| 将微调后的同一分类/回归任务交给v1实验接口 | 监督模板导出和返回评价一致；蛋白质CSV与ESOL实际往返运行、绘图均完成 | 使用原版GPT类和课程监督模板；自主改码实验尚未通过 |
| 原版autoresearch、v1、v2完整研究 | 固定源码和教学配置已准备；v1想法与反思、v2工具与图像调用已有实际记录；v1显式兼容路由已实测 | 长改码请求仍失败；完整写作、研究树、停止恢复尚未验收 |
| 学生在CLI中自然对话、连续完成三段 | 受限沙箱已能执行命令，部分新教学已实际运行；全部场景尚未通过 | 本机记忆访问、每个Skill的自有/私有数据、GLM兼容性和三段贯通需要分别验证 |

这些是直接执行命令得到的记录，不是新一轮学生—Agent完整对话。旧对话原文不改写，也不拿旧回复证明新版Skill已经通过。

## 十类数据的微调

本次每个任务使用最多32条训练样例和16条验证样例、两轮微调。所有模型都检查了参数确实变化、源checkpoint没有变化。

| 数据 | 评价指标 | 微调前 | 微调后 |
|---|---|---:|---:|
| 文本 | 教学分类准确率 | 1.0000 | 1.0000 |
| 蛋白质 | 教学分类准确率 | 0.5625 | 0.4375 |
| DNA | 教学分类准确率 | 0.6250 | 0.8750 |
| SMILES | 溶解度MAE | 1.6025 | 1.9677 |
| 天气 | 生成样例平移速度MAE | 0.02969 | 0.02882 |
| 晶体 | 生成样例密度代理量MAE | 0.74324 | 0.68357 |
| 三维结构 | 生成螺旋间距MAE | 0.35905 | 0.29166 |
| 图像 | 生成图像源数量MAE | 1.35828 | 1.18324 |
| 光谱 | 生成黑体温度MAE（K） | 1384.56 | 1141.03 |
| 连续物理场 | 生成扩散系数MAE | 0.04026 | 0.03843 |

“微调前”是预训练模型接刚初始化的任务头，不是训练充分的冻结表示对照。标签来源与单位见[原始结果](training-ten-domains-2026-09-05/acceptance.json)。文本分类只是可选的迁移示例，讲问答时用下节的SFT，不用分类结果代替。

22条命令（基线、十类预训练及微调、20步文本SFT）顺序运行，全部退出码0；本机单线程CPU命令耗时合计158.764秒，不含后续补测，不保证其他电脑相同。[完整命令与耗时](training-ten-domains-2026-09-05/commands.json)、[逐条stdout和stderr](training-ten-domains-2026-09-05/)已保存。当前重跑脚本已将SFT预算改为200步，因此会与第一轮20步记录不同。

```powershell
python scripts/verify_training_lesson.py --output out/my-training-check
```

输出目录必须是新的。这个脚本由教师验收使用，学生只运行自己选择的领域。

## 文本问答：原题与换个问法分开

最初20步的回答仍是空行，保留在第一轮记录中；这不能作为好用的问答演示。随后在同一个预训练checkpoint上重新做200步SFT，未改训练题或验证题，另存结果。

实际命令：

```powershell
python -m nanoscigpt.tasks.text_sft --ckpt out/training-complete-20260905/training/text/model/ckpt.pt --out_dir out/sft-final-200
```

| 输入 | 微调前实际输出 | 微调后实际输出 | 属于哪种检查 |
|---|---|---|---|
| `What is DNA?` | 仅换行 | `A sequence of bases.` | 训练原题 |
| `What is a protein?` | 仅换行 | `A chain of amino acids.` | 训练原题 |
| `Describe a protein.` | 仅换行 | `Lig t arnom aata.` | 改写问法，回答失败 |

八道训练原题都能生成对应答案，但三道改写题均未正确回答。回答部分验证损失从3.12893降到2.93996，也不等于问答能力合格。课堂可以据此讲清“改变训练目标”“拟合训练例子”和“对新输入泛化”的区别，而不能宣传训练出了科学问答模型。[全部原题、参考答案和实际生成文本](training-ten-domains-2026-09-05/text-sft-200.json)分别保存。

## 原版v1接续蛋白质模型

适配器不替换原版GPT或训练循环，仅接入课程数据、权重和小规模设置。测试先检查同一输入的预测数值一致，再由原版实验代码继续训练。实际课程模型也单独运行了一次，不只测试随机初始权重。

- 源码：SakanaAI/AI-Scientist，固定commit `1de1dbc1f4ee2c5f61e9c94348d55eb51d7fa2eb`。
- 输入：本轮A线实际训练的蛋白质checkpoint，hash保存在[运行记录](native-protein-continuation-2026-09-05/record.json)。
- 原版实验内部验证损失：2.9211 → 2.9013。命令耗时4.735秒。
- 这里只比较这次原版实验内部的同口径评价，不与A线另一套抽样结果直接作差。
- [实际stdout](native-protein-continuation-2026-09-05/stdout.txt)包含加载课程权重、训练和保存记录；[stderr](native-protein-continuation-2026-09-05/stderr.txt)保留原版弃用提示。
- `agent_api_run: false`：这不是想法→改码→实验→写作全流程，不能用本次基线代替原版AI Scientist验收。

补测也已覆盖[文本](native-text-continuation-2026-09-05/record.json)、[DNA](native-dna-continuation-2026-09-05/record.json)和[SMILES](native-smiles-continuation-2026-09-05/record.json)，均退出码0，保留各自输入模型hash与原版实验结果。它们同样不是Agent API研究验收。

## 回归检查

全套143项测试通过（308.30秒），[测试报告](training-ten-domains-2026-09-05/verification.xml)已保存。此前原版v1测试错误地要求每次实验都保存更优模型，已依据上游“仅在指标改善时保存”的行为修正，不通过强制改善或固定有利种子来让测试通过。

随后修正对话记录脚本：沙箱错误同时检查stderr和原始事件，失败返回非零退出码，分别记录尝试轮次与完成轮次；记录成功本身不代表教学实验成功。相关14项测试再次通过（12.85秒），[补测报告](training-ten-domains-2026-09-05/verification-extra.xml)已保存。两组测试有重叠，不能简单相加；其中对话捕获的模拟事件测试不是模型真实回复。

三个Skill格式检查通过，当前入口文档的本地链接无缺失，`git diff --check`通过。修改保存在本地教学打磨分支，尚未提交或推送；历史原文未改。

## 未解决的上线条件

1. **原项目模型服务**：v1显式兼容路由已修复并实际调用；长改码请求仍有超时和截断，需要定位真实研究请求的问题、设置整场预算并验收完整流程。GLM文本调用不自动等于v2视觉反馈可用。用户此前提供的SCNet接口文档在原桌面路径及本课程工作区未找到，不能猜测其服务地址或密钥。
2. **连续CLI教学**：Windows受限沙箱解析故障已获授权修复。仍要在无持久记忆的全新会话完成三Skill全部场景及模型兼容性检查，不以刚恢复的读文件能力或有旧记忆的成功示例替代。
3. **autoresearch运行环境**：当前教学Python是CPU版PyTorch；原版需要相应CUDA环境和数据。没有为赶进度自动安装大型GPU依赖或启动无限循环。

待这些条件满足，才验收三个原项目的实际自主研究及完整课堂对话。没有据此宣称“所有数据、所有电脑、全部研究流程都完备”。

### 此前自动继续停止记录（现已获确认）

在提出原版失败保留/状态修补与沙箱修复确认、补充微调备用材料、再次复核这三轮中，确认请求持续未获用户答复。自动目标续跑消息不作为同意。上一轮备用材料是实质产物；本轮检查没有解除关键阻塞，不算新的实验或修复完成。

再次读取沙箱状态仍为22个零字节，修改时间未变；按教学对话、改码探针和研究启动脚本名称检查，没有匹配的正在运行进程，因而不是等待某个仍在运行的实验。原SCNet文档路径仍不存在。未修改全局沙箱、未放宽权限，未为达到通过结果而重新启动长API请求。

本地可交付部分继续保留，完整目标转为阻塞等待用户确认，不再自动重复搜索、检查或补写同义状态。恢复后首先处理已提出的原版失败保留/失败状态修补和受限沙箱修复；随后仍须落实原版autoresearch所需CUDA环境、完整研究实测、连续CLI教学及发布验收。目标范围不缩减，PPT仍由另一个会话处理。

用户随后回复“继续”，本轮据此完成上述两项修复，见本页顶部的新记录。上面三轮停止说明仅记录当时的决策，不表示现在仍在等同一次确认。
