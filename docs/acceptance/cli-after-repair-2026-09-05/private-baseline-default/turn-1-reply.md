我理解为：**每行是一种材料配方，用组分比例和制备温度预测强度**，这是表格回归任务。先按“预测未见过的配方强度”来设计实验。

按仓库这份 skill，数据不能上传时，我们先完成实验设计。你不需要提供文件路径或数据样例；目前没有读取你的数据，也没有运行训练或生成分数。

1. **先在本地检查表格。** 确认强度和温度各自使用统一单位，组分比例统一采用 0–1 或百分数，并检查空值、重复配方。强度缺失的行先留作待预测对象，不用于训练；组分空值不要直接当成零。

2. **划分训练集和测试集。** 可以先留出约 20% 配方作为测试集，其余用于训练。同一配方若有重复记录，应放在同一组，避免训练时已经见过测试配方。测试集先留着，不用于调参。

3. **确定第一轮对照。** 一个简单对照是“对所有测试材料都预测训练集的平均强度”；第一个模型用 **RandomForest 回归**，输入仅包含组分比例和制备温度。

4. **提前约定怎样判断结果。** 主要看 MAE，即预测强度平均偏差多少，单位与强度一致；同时与平均值对照比较，并查看哪些配方误差最大。随机留出的结果只说明当前数据范围内的预测能力，不能直接证明能预测全新的材料体系。

**现在先做第 1 步。** 在本地记下总行数、强度缺失行数、是否有重复配方，以及单位是否统一。这些检查无需上传数据；完成后，我们再决定划分方法。