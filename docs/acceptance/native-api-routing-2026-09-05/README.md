# v1 兼容接口路由：已修复并实际调用

2026-09-05。本轮只修模型路由，不改研究内容、训练数据、实验循环或写作提示词，也不改 PPT。

本轮最终全量检查：**176 passed in 356.97s**，0 失败、0 错误、0 跳过；[完整 XML](full-tests.xml)。运行的是仓库全部 `tests/`，不收集 `out/` 里的第三方项目测试。它包含旧离线演示和接口边界测试，不能被解释为完整原版研究或连续 CLI 教学已通过。

## 修复对象

上一轮原版 `create_client('scnet/GLM-5.3')` 在联网前就报不支持模型。另有两个评阅入口硬编码使用其他模型；只改 `--model` 无法覆盖它们。

新增 `python -m nanoscigpt.upstream configure-api v1 --model ... --review-model ...`，在固定版本副本上显式配置，保存[完整差异](teaching_api_changes.diff)和[模型选择、源码摘要](teaching_api_setup.json)。用法见[适配说明](../../upstream-adaptation.md)。所选模型名原样传输，Aider 使用明确的兼容接口前缀；辅助模型不再为空。非 GPT 模型保留逐次多回答方式，避免擅自要求批量 API，也避免原版别名映射替换实际模型名。

只允许修改已核对的两份原版源码；发现学生改动、既有未完成的补丁、配置后文件变化或模型选择不同，均拒绝覆盖。没有保存密钥或修改全局配置。`configured_not_run` 不代表研究完成。

## 实际调用

采用本机此前验证过的兼容服务；默认源码修订仍是 `1de1dbc1f4ee2c5f61e9c94348d55eb51d7fa2eb`，本次补丁另行公开，不再声称文件完全未改。

| 测试 | 真实输出 | 耗时 |
|---|---|---:|
| GLM 单次回答和两次连续回答 | `OK`；`["OK", "OK"]` | 7.890 秒 |
| 默认模型 gpt-6-astra 单次回答 | `OK` | 4.625 秒 |

[运行摘要](record.json)、[请求正文](requests.json)、[响应正文](responses.json)保留原文。共四次请求；只测路由，不把回答 OK 作为改码或科研能力的证明。该探针设置 30 秒单次超时、256 输出额度，并在运行时绕过重试装饰器以限制探针；适配后的原项目源文件本身没有修改重试策略。

原版 `launch_scientist.py --help` 在独立 Aider 环境实际执行成功，识别 `scnet/GLM-5.3` 和 `gpt-6-astra`。这只是启动器加载和参数入口验收，没有启动实验或论文写作。

## 回归

新增测试先复现无适配入口、别名替换和既有补丁被覆盖的问题，再完成修复。先完成[相关测试](tests.xml)：**17 passed in 38.44s**，覆盖路由、配置命令、改动保护、原版模型预测一致性、课程权重继续训练与监督任务接续；随后完成页首的 176 项全量检查。没有跳过本次原版模型集成测试，两组测试不相加，也不等于真实 Agent 对话次数。

全量命令：`python -X utf8 -m pytest tests -q --junitxml=out/native-api-final-20260905/full-tests.xml`，运行进程设置 `OMP_NUM_THREADS=1`、`MKL_NUM_THREADS=1`，未修改系统环境设置。

测试中的接口传输使用本地替身，真实网络验证单独以上面的四次请求为准。依赖仍有原有 Requests 警告及 Google 旧 SDK 弃用提示，未为消除提示升级系统环境。

## 剩余问题

本记录生成时，失败目录保留和CLI沙箱修复尚未完成。用户后来确认继续后，已完成[失败处理修补](../native-failure-repair-2026-09-05/README.md)和[沙箱修复](../cli-sandbox-repair-2026-09-05/README.md)，[真实CLI复测](../cli-after-repair-2026-09-05/README.md)另行记录，不再等待同一项授权。

长请求超时、输出截断、整场预算、完整实验写作与v2研究树仍未验收。[关闭推理参数的实际改码检查](../native-coder-thinking-2026-09-05/README.md)也未产生代码，不把路由短答成功当作完整研究可用。当前修改未发布到GitHub。
