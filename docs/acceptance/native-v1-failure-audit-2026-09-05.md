# 原版 v1 失败处理复核

状态：源码审计，尚未修改研究控制逻辑；不是完整研究运行验收。

后续：用户确认后已实施[失败保留与完成状态修补](native-failure-repair-2026-09-05/README.md)。以下为修补前固定原版的审计，不覆盖或改写历史证据。

检查对象为本地固定版本 SakanaAI/AI-Scientist，commit `1de1dbc1f4ee2c5f61e9c94348d55eb51d7fa2eb`。本轮核对 HEAD，并确认 `ai_scientist/perform_experiments.py` 相对该版本没有改动。以下行号均为该原版文件，不是课程另写的研究演示。

## 会影响课堂验收的行为

| 位置 | 实际代码行为 | 对课堂的影响 |
|---|---|---|
| `run_experiment`，52—55、79—84行 | 非零退出或超时后调用 `shutil.rmtree` 删除 `run_N/` | 原版保留了 `run_N.py` 代码副本，但当轮目录中的部分结果会被删除；仅在 Skill 中要求保存失败不足以保护这些文件 |
| `perform_experiments`，154—166行 | 绘图失败达到重试上限后退出绘图循环，随后写 notes 并返回 True | 如果后续 notes 调用不抛异常，绘图失败本身不会让此函数报告失败；不能据其 True 宣称图表完成 |
| `perform_experiments`，130—134行 | 检查模型文本中的 `ALL_COMPLETED` 后直接退出实验循环 | 该返回信号本身不证明运行过新实验；教学验收需检查实际结果文件，不以模型自报为准 |
| `run_experiment`，30行 | 单次实验默认 7200 秒 | 小模型步数限制不等于整个研究的墙钟/API预算；该函数的超时也不约束外面的 `coder.run` |

原版 `launch_scientist.py` 221—229行会读取实验函数返回值，False 时停止当前 idea；以上第二项因此需要单独处理，而不能期待 launcher 自动识别所有绘图失败。这里不声称上游所有版本均有相同行为。

## 当前可确认与不可确认

- 已有全量报告再次读取为176项通过、0失败、0错误、0跳过，耗时356.973秒。本轮只是读取该报告，没有再跑一遍；见 [报告](native-api-routing-2026-09-05/full-tests.xml)。现有通过记录不覆盖本次指出的原版失败处理要求。
- v1 兼容模型路由和真实短调用记录见 [接口验收](native-api-routing-2026-09-05/README.md)。这不证明长改码、实验、写作已经全部跑通。
- 本轮未改全局 Codex 沙箱。其 `deny_read_acl_state.json` 仍为22个零字节，修复授权仍待用户确认。
- 历史已跟踪 CLI 对话文件相对 HEAD 无差异；未覆盖用户导出的原文。
- 未提交、未推送，也未完成远端干净克隆验收。

下一项实现应只处理原版执行的失败保留与失败状态，不改变想法生成、研究任务或研究路线。整场停止机制和原版长调用仍须另行实际验证，不能把这次源码审计标为修复完成。
