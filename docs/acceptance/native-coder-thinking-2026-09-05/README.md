# v1原版改码：关闭推理参数的有界检查

2026-09-05。结果仍为**失败，没有代码改动**，不继续提高输出额度。此前短请求已验证模型名路由，本次检查的仍是原版实验提示词与Aider diff改码流程，而不是“回复OK”。

在独立`out/`副本中，沿用实际生成的研究想法、相同教学蛋白质数据与任务、同一初始权重；先真实运行基线，MAE为3.398047924。仅这次探针向Aider模型的`extra_params.extra_body`加入`thinking: {type: disabled}`；6000输出额度、90秒API超时、一次请求、关闭外层传输重试、禁止自动执行模型建议命令。没有改全局模型配置。

参数形式参考[服务商思考模式文档](https://docs.z.ai/guides/capabilities/thinking-mode)。该文档不能证明本机SCNet兼容路由会原样传递或支持此参数，必须以实际返回为准。

## 实际返回

| 项目 | 结果 |
|---|---|
| 模型 | openai/scnet/GLM-5.3 |
| 改码探针耗时（含初始化） | 74.609秒 |
| finish_reason | length |
| completion_tokens | 6000 |
| 其中reasoning_tokens | 5998 |
| 最终正文长度 | 3字符，见[回答原文](answer.txt) |
| 代码改动 | 无，`changes.diff`为空 |
| 数据、任务配置、初始权重及GPT源码 | SHA256均未变化 |
| 新实验执行 | 未执行；只运行了改码前基线 |

[运行摘要](record.json)、[原实验输入](input.txt)、[探针源码快照](probe.py)保留原文。源码保留原`out/`路径假设，是诊断记录，不是学生入口。完整请求和响应保留在本地`out/native-coder-thinking-20260905/run/`；响应含模型推理内容，不将其当作可运行代码或教学回复。

这里能够确认的是：请求中提出关闭推理，但实际响应仍大量消耗推理token，并被截断。尚不能区分兼容层是否丢弃参数、上游是否忽略参数或其他路由行为；不据此断言GLM模型不能编程。要继续验收原版完整研究，需要核对实际编程请求的兼容链路，不能用接口短答成功替代。
