# 课堂版本发布检查

2026-09-05。首页按“安装—三个Skill提问—运行命令—输出位置”重新整理；原版autoresearch、v1、v2完整研究明确列为未验收，不用历史规则演示替代。

- 发布前全量回归：[186项通过，361.08秒](full-tests.xml)，0失败、0跳过。测试使用本机Python 3.12与已有依赖，不能等同所有学生电脑均通过。
- README的本地链接检查通过。
- 拟发布文件扫描未发现已配置用户环境密钥的原值或常见密钥格式；这不是穷尽所有秘密检测。
- 旧CLI第二轮JSONL含本机记忆读取内容，仅保留在本机，不上传；原文件未改写，摘要和位置见[说明](../native-route-dialogue-2026-09-05/STATUS.md)。
- 源码与当前使用说明的空白检查通过。部分原始对话/日志保留尾随空格和结尾空行，不为格式检查修改历史证据；全范围`git diff --check`因此仍有这些记录格式提示。
- 本次不修改PPT、网页、全局CLI/代理配置，也不发布本机训练权重或`out/`中的实验目录。

## 干净克隆复查

从实现提交`7f3a9efb681e59a295925c2723c7c9489f97001b`克隆到新目录，创建独立虚拟环境。为了不重复下载依赖，虚拟环境复用本机system-site-packages，安装使用`--no-deps --no-build-isolation -e .`。这是干净源码与数据复现，不是全新电脑的联网依赖安装验收。

按README的CPU命令，以下步骤均退出0，且实际输出文件齐全；运行后克隆目录Git状态干净：

1. 创建虚拟环境、安装课程包。
2. 十类课程数据预检：[返回记录](data-list.txt)。
3. LAMOST回归：[评价](baseline-metrics.json)。
4. 文本预训练与200步回答微调：[实际结果](text-sft.json)。
5. 蛋白质预训练与任务微调：[实际结果](protein-finetune.json)，accuracy从0.5625变为0.4375，保留退步结果，不据此声称训练收益。

[逐条命令、耗时和退出码](clean-clone-record.json)。三个Skill格式检查通过。这次是直接执行README命令，不冒充新的学生—Agent连续对话；原版AI Scientist完整改码、实验和写作依旧未通过。随后的补充提交只增加本发布记录，不改变上述已测试的生产代码。
